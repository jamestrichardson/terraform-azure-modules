# Log Analytics Example

Deploys a resource group and a Log Analytics workspace using the `log-analytics` module.

## Usage

```bash
terraform init
terraform plan
terraform apply
```

## Files

- `main.tf`: resource group + module instantiation
- `variables.tf`: configurable inputs
- `outputs.tf`: key outputs from the deployment
