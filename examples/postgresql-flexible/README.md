# PostgreSQL Flexible Server Example

Deploys a resource group, Log Analytics workspace, and PostgreSQL Flexible Server with one database.

## Usage

```bash
terraform init
terraform plan
terraform apply
```
